# arma-2-Object_Placer

ArmA 2 Object placer, stub at the moment, prints object coords into the screen (I think)

Reads MASK satellite image, then creates QGraphicsView and randomly places ellipses into the colored areas.
